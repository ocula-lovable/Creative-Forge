## Packages
framer-motion | Smooth layout transitions and entry animations
recharts | Analytics charts for the dashboard
date-fns | Formatting dates for projects and assets
clsx | Conditional class names
tailwind-merge | Merging tailwind classes for components

## Notes
- API uses /api/projects and /api/assets
- Generation endpoint is /api/generate
- Authentication is handled via /api/user and Replit Auth
- Dark mode is the default and primary theme
